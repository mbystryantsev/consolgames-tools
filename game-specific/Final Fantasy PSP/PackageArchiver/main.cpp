#include "LZ77.h"
#include <iostream>
#include <algorithm>

void printChar(int v)
{
	std::cout << v << ' ';
}

int main()
{
	//std::vector<int> v = LZ77Decoder::calcPrefix("abcdabscabcdabia");
	//std::for_each(v.begin(), v.end(), printChar);
	return 0;
}